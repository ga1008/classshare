# 职业与简历验收台账

本台账对应调查方案的 A01–A26，区分本地证据与上线门槛。当前未部署；“本地通过”仅适用于表中已测条件，不能据此认定生产验收完成。测试数据均为合成或公开岗位文本，不使用学生真实简历作为压测数据。

| 编号 | 当前结论 | 对应证据与剩余边界 |
|---|---|---|
| A01 | 本地主链路通过，生产计时待测 | 新专业基础方向、问卷及简历入口不依赖 AI 完成；真实 HTTP 14 条主流程通过；最终冲突 UI 另 4 条 HTTP 链路通过 |
| A02 | 隔离 PostgreSQL 通过 | 100 学生、20 并发初始化同一专业只创建一个任务；`career_postgres_workflow_probe.py`，最终紧凑协议另以 20 学生复验 |
| A03 | 回归通过，真实 AI 样本继续记录 | 超时、429、坏 JSON、预算耗尽和有效旧图保护；`test_career_lifecycle.py`、`test_student_career_jobs.py`、`test_career_compact_generation.py` |
| A04 | 回归与真实 PG 进程强杀通过 | 4 个关键检查点强杀后可恢复；已保存候选无需重算；仅推进隔离测试租约，生产恢复时间待实际灰度核对；见 `career-process-kill-evidence-2026-09-06.json` |
| A05 | 浏览器隔离检查通过 | 单在途、离线/慢响应、401/服务不可用、停止轮询；`tests/frontend/career_resume_browser.test.cjs`，最终实际 HTTP 主流程通过 |
| A06 | 隔离 PostgreSQL 通过 | 1000 次已初始化 GET 无任务、会话写入；轻量状态跳过完整材料和图谱读取 |
| A07 | 自动样本通过，内容评审待完成 | 77 样本覆盖七类专业和未知专业；未知不回落软件。实际跨专业生成另测，11 个通用框架样本仍需专业内容扩展 |
| A08 | 业务回归通过 | 学制缺失不猜测、专升本/毕业阶段、学籍与专业变化使旧任务失效；生产真实学籍边界待灰度核对 |
| A09 | 业务及浏览器检查通过 | 两种问卷模式恢复、revision、双击和非法答案；完整问卷真实 HTTP 及 300 人/30 秒接纳通过 |
| A10 | 业务及数据库回归通过 | 乱序保存 409、重复命令合并、重测与迟到结果围栏；真实 HTTP 冲突保存保留输入及备份恢复通过 |
| A11 | 自动对照通过 | 明确证据、城市、阶段和收藏/屏蔽改变基础推荐；同输入同版本可复现，不以文本差异作为依据 |
| A12 | 反例回归通过 | 意向不算技能；否定证据不命中；真实 JD 的届次、分数和 OR 条件未支持时保留待确认 |
| A13 | 自动反事实与隔离回归通过 | 姓名、性别、隐藏支持材料不参与基础评分；公开任务字段白名单，不返回他人结果和原始供应商异常 |
| A14 | 未通过：需要专业评审 | 77 样本自动报告不能替代人工 Top-5 质量评分；≥90% 门槛尚未测量，不伪造专家结论 |
| A15 | 来源设施与时效回归通过，生产数据待接入 | 三条当前官方岗位候选已逐条核验，仍是单一雇主；尚未生产导入或建立长期维护接口 |
| A16 | 本地及隔离 PostgreSQL 通过 | 同名不同 JD、版本、个人目标鉴权和定向输入；目标归档不改变已确认历史 |
| A17 | 真实 HTTP/PG 通过 | 保存接口签名修复、草稿保存、source_context、顺序与目标保留；头像 revision、摘要/能力分组编辑与源材料隔离经最终 HTTP 复验通过 |
| A18 | 业务、隔离 PostgreSQL 与真实强杀通过 | 编辑 revision 与候选确认 CAS、旧结果不能覆盖、冲突和历史恢复；强杀 apply 事务后，新 revision 仍阻止旧候选发布 |
| A19 | 版本、缓存与 20 份真实导出通过 | 冻结版本预览、缓存内容键和渲染器版本、跨进程互斥；导出忙返回 429/Retry-After |
| A20 | 最终三模板视觉与事实矩阵通过 | v3/v4 技能连字和侧栏孤标题已修复；v5 侧栏 PDF 5 页、Word 重渲染 6 页独立逐页通过；另两模板 HTML 与已检查的 10 张 Word 页图字节一致。三模板×四表面×33 事实共 396 项通过，九类经历全文及次数、证书日期/描述/链接、头像均核对；见 `career-resume-final-fact-evidence-2026-09-06.json` |
| A21 | 边界与业务回归通过，生产重启待测 | 原件保留、候选选择、重复/冲突和解析资源上限；异步任务故障与重启由专项补测，不以成功上传证明解析成功 |
| A22 | 事务与跨域引用回归通过 | 同 hash 引用保护、PG 两种锁顺序、原件/头像/课程/不可变快照；逻辑删除后保留共享字节，不进行历史自动物理回收 |
| A23 | 真实 HTTP/PG 通过 | 投递绑定简历版本和 JD 快照，修改原简历/归档原目标后仍保持历史 |
| A24 | 服务/路由隔离与最终 HTTP 矩阵通过 | 以其他学生 ID 检查资料、简历、附件、预览、导出、任务、候选、历史、目标与投递；最终真实 HTTP 逐项核对 GET 子资源 404 和修改拒绝 |
| A25 | 结构和浏览器回归通过 | 名称和 ID 去重、颜色白名单、坏边拒绝、HTML 转义、计时器清理；修复空筛选仍保留旧图 |
| A26 | 已测布局与键盘通过，完整门槛待补 | 360/768/1440 宽、720×450 的 200% 等效重排、键盘与减少动画通过；390 宽与浏览器原生 200% 缩放尚无独立最终证据，不把等效重排冒称原生缩放 |

## 性能、稳定性与上线门槛

| 项目 | 当前已测 | 待完成 |
|---|---|---|
| 50 RPS/1000 学生 | 桌面隔离 PG/ASGI 60 秒，含每人材料和冻结简历，0 非预期响应 | 1000 人/60 秒冷专业波峰通过，接纳 P95 15.239ms；v5 固定版本一小时混合已实际完成，21.95万请求，0意外/5xx；相对教学延迟门槛失败，见一小时专项报告；同规格独立环境真实 HTTP 待测 |
| 命令队列 | 1000 任务、8 worker，峰值执行 2、每项生效一次；另有真实 HTTP 同专业合并 | 300 人/30 秒完整问卷接纳通过，P95 23.138ms；10 个共享及 300 个个人任务逐条核验；一小时教学/提醒混合已完成；停止时仍有89任务，排空另测 |
| 简历编辑与导出 | 100 写者短负载保存 P95 34.207ms；合成进程转换容量/去重通过 | 20 份实际首次转换及 20 次缓存读取已完成（83.987 秒）；实际 converter 峰值 1，忙状态 21 次均重试成功，残留运行进程/槽记录 0；与负载分开执行 |
| 教学和提醒混合 | 原有头像接口只能代表轻量资料查询；P95 5.617→8.984ms，未达到相对恶化≤20% | 一小时真实课程与提醒完成；120次提醒P95为18.285秒；课程P95增加88.56%、头像增加90.17%，原≤20%门槛未过。发现同步资源采样阻塞事件循环，正修复工具后复测，原报告保留 |
| 资源 | 桌面短负载无池耗尽、池等待很低；不是线上规格 | 同规格 2 CPU/约3.57GiB、内存余量、CPU、DB 锁等待与积压；不能在生产直接施压 |
| Office 故障 | Windows 真实子进程强杀、子树归属、容量槽残留与截止时间恢复通过 | Linux/POSIX 实际故障验收；当前本机无可用 WSL/Linux 环境 |
| 长时稳定性 | 固定v5一小时完成，但混合相对延迟及排空未通过；24小时未开始 | 固定代码下 1 小时混合与 24 小时低强度故障浸泡，不以缩时实验充数 |
| 迁移与部署 | 线上只读数量/状态盘点、旧 DDL 隔离 PG 迁移/中断回滚/重复回填通过 | 1,820 文件 DryRun 通过，证书备份等无关文件已排除；最终代码冻结后再做发布 DryRun；增量备份迁移、灰度和失败专业线上复验待执行 |

具体运行数值及证据状态见 [实施记录](career-development-implementation-status-2026-09-06.md)。原始完整门槛见 [调查与改进方案](career-development-audit-and-plan-2026-09-06.md)。本台账不会把尚未执行的检查勾选为完成。

2026-09-06 发布范围调整：用户要求停止扩展测试并部署提交。以 career-release-status-2026-09-06.md 记录当前完成度、未过指标与未执行项目；不再以原24小时/人工质量门槛阻塞本次发布。
