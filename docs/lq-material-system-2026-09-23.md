# 全站共享玻璃材质与接入契约

本次按用户提供的 iOS 截图统一所有交互表面，覆盖内容面板，不再沿用旧设计稿“内容必须是不透明纸面”的限制。白板笔迹、签章、试卷打印预览和正式导出是内容数据，保留原色与纸张语义。

## 模块化的边界

1. `static/css/lq/tokens.css` 是基色、透明度、圆角、模糊、层级和时长的唯一参数来源，浅深主题都由同一套语义名解析。
2. `materials.css` 定义五档材质和公共属性接口；`components/material-boundaries.css` 决定如何渲染霜面及如何降级。页面不复制模糊算法。
3. Jinja 宏、原生 JS 工厂和 React 组件继续共享 `lq-*` 组件；菜单、对话框和抽屉使用现有 LQ layer 管理 portal、焦点、Escape 和滚动锁，不新增第二套协调器。
4. 页面 CSS 只保留尺寸、布局、密度和业务状态；独立考试、编辑器、批改、课堂与简历均明确接入材质。新增页面声明属性即可，无需修改共享模块内的页面名单，也不使用 MutationObserver 遍历业务 DOM。
5. 模板、数据与业务控制器保持边界。此次不会重写草稿、提交轮次、权限或评分 API；对明确的交卷重复请求增加控制器防重入。每个业务修复保留专门回归证据。

## 五档材质

| 属性值 | 用途 | 浅/深基色不透明度 | 模糊策略 |
|---|---|---|---|
| `clear` | 有独立对比保护的图片浮层 | 22% / 22% | 16px；低能力设备升为厚材质 |
| `control` | 按钮、输入、选择、胶囊 | 52% / 54% | 使用承载层的霜面，不逐控件创建过滤层 |
| `chrome` | 顶栏、侧栏、操作条 | 58% / 62% | 16px，实时模糊其下滚动内容 |
| `content` | 题干、列表承载区、卡片组 | 74% / 76% | 桌面预生成霜纹；触屏顶层承载区 8px |
| `raised` | 弹窗、下拉、抽屉 | 78% / 80% | 24px，实时模糊页面内容 |

浅色基色为白色，深色为 `224 30% 12%`。语义状态颜色只调整色调，不能把危险操作、保存失败、零分或只读状态装饰掉。内容字号、行距和对比优先。

```html
<section data-lq-material="content" aria-labelledby="task-title">
  <h2 id="task-title">本周任务</h2>
  <!-- 使用现有按钮组件；行和按钮不再独立创建模糊边界 -->
</section>
```

重复数据列表把材质放在承载区；不能在每条数据上创建新的模糊边界。浮动菜单交给共享 layer 移出祖先的过滤/裁剪上下文。直接使用 `data-lq-interactive` 的可操作材质遵守统一 hover、press、focus；禁用态不抬升，触屏不保留悬停光晕。

## 背景与性能

所有 8 个用户文档根共用 `partials/lq_page_backdrop.html`，每页只出现一次。背景复用开屏欢迎页图库与账号偏好，固定铺满视口，`cover` 保持比例并居中裁切。无需每页额外随机加载一张图。桌面内容预模糊图与原图共享视口坐标；手机不使用无法可靠对齐的 fixed background，而由显式内容边界取样。

入场只改变背景 opacity，避免缩放导致背景与霜纹错位。不循环播放、不常驻 `will-change`。内容内的嵌套面板和控件禁止叠加 blur，数据增长不会增加列表行的过滤层。真正浮起的导航/菜单使用实时模糊，不能拿背景贴图遮盖正文来冒充透视。

无 JS、低能力、关闭透明、减少透明、高对比、强制配色和打印都有可读退路；不能要求浏览器识别一个可选媒体特性才启用正常材质。减少动效时取消入场与位移。图库请求失败不影响正文、提交或导航。

## 维护与验收

全模板根/继承/消费者表见 `lq-route-audit-2026-09-23.md`。静态覆盖、浏览器行为与业务验收分开记录；旧迁移注册表的历史状态不自动升级为“全部验收”。

- 共享边界：`tests/e2e/components/lq-material-boundaries.spec.ts`。
- 师生/桌面触屏/浅深矩阵：`tests/e2e/specs/lq-unified-material.spec.ts`。
- 考试作业：`student-assessment-material.spec.ts` 与原有 S3 草稿、409、附件、交卷用例。
- 教师课堂、学期操作、四编辑器及批改：`lq-teacher-materials.spec.ts`。
- 简历与博客：`lq-resume-blog.spec.ts`，简历既有保存/导出/冲突组件测试。

使用 `python tools/test_backend.py` 隔离后端测试。浏览器运行在各自新建的 synthetic SQLite fixture；不启动生产 worker，不连接本地真实业务库。发布继续使用当前源码匹配的原生 PostgreSQL 演练报告和 dump，先 DryRun，再停写备份和部署，最后核对线上版本、资源图与健康。

浏览器触屏模拟不能替代真实 iPhone/Safari 性能验收；该限制需随发布记录保留。
