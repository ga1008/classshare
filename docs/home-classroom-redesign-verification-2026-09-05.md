# 首页与课堂改版：最终验收记录

日期：2026-09-05。对照[原97项验收标准](home-classroom-redesign-acceptance-2026-09-05.md)。基线 `dev / c79f1674`；本轮改动位于共享工作区，未提交、推送或部署。原调查与设计理由见[完整方案](home-classroom-redesign-plan-2026-09-05.md)。

本记录更新此前65项通过的阶段报告；阶段记录保留在证据目录 `continuation/verification-before-closure.md`，其中的未测说明和旧计数不再代表当前状态。下表逐项保留证据范围。**本地实现验收与发布验收分开：P06仍须staging验证，P08只能上线后采样，不宣称97项全通过。**

## 证据索引

“证据目录”均指[artifacts/home-classroom-redesign-2026-09-05](../artifacts/home-classroom-redesign-2026-09-05/)，该目录gitignored，保留可审阅报告、截图、脱敏JSON和复现脚本；不包含口令、cookie或生产数据库。

| 别名 | 位置及实际范围 |
|---|---|
| U1/U2/UC/UM/UI/UE | `tests/test_dashboard_workspace.py`、`test_dashboard_workspace_api.py`、`test_classroom_workspace.py`、`test_session_learning_materials_service.py`、`test_frontend_authenticated_vite_integration.py`、`test_ui_explanation_system.py`；最终PY146联合回归覆盖这些模块 |
| TC/TD | `frontend/src/lib/classroom-workspace.test.ts`、`assignment-clock-runtime.test.ts`、`classroom-bootstrap-ready.test.ts`、`dashboard-workspace.test.ts`；最终Vitest157项/29文件 |
| BK | [后端完整路由补证](home-classroom-backend-closure-2026-09-05.md)，`backend-closure/`；11个真实登录/SQL/模板/权限case，8模块114项回归通过，固定时钟精确断言 |
| BT | `backend-clock/report.md`、`results.json`、12PNG；两页统一时钟、无需document重载、六阶段真实Chrome/API联合验证 |
| BUS | [业务操作报告](home-classroom-business-verification-2026-09-05.md)，`continuation/business-workflows/`；真实提交、保存、回读、受限下载；逐条最后成功日志 |
| MAT | `continuation/classroom-material-workflow/report.md`；真实绑定7、受控生成3、原服务单测54，生成POST拦截不调用真实AI |
| AX | [布局与无障碍报告](home-classroom-a11y-layout-validation-2026-09-05.md)，`continuation/a11y-layout/`；容器/长文字/缩放/两校区/数字对比/键盘/加载前操作 |
| PF | `backend-fullpage/report.md`；修正时间注入后F02/F04完整路由trace、gzip、SSR、cold/hot请求。旧740样本已撤回同时间比较资格 |
| CP/HP | `continuation/classroom-performance/report.md`、`continuation/home-performance/`；明确CPU/网络的浏览器LCP/CLS/真实交互，原始偏差保留 |
| BE/BW/BQ | 六份既有spec及workspace、二维码专项；`final-browser.log`、`final-workspace-browser.log`、`classroom-group-qr/`保留历史通过，最终受影响回归另见执行登记 |
| BC/BH | `classroom-probe/final.json`及会话内CUA观察；BC为独立归档，BH为手工观察（无独立PNG/JSON），不当成自动测试 |
| FC/FN | `final-comparison/`：同初始SHA两份合成SQLite、64PNG、280次导航；`final-current/`：阶段末32PNG。均为单课堂历史范围，非完整F02/F04最终性能 |
| SQ/PG | [后端专项报告](home-classroom-backend-verification-2026-09-05.md)、`postgres-verification/`；SQLite查询边界与真实本地PG16.14读取/并发，不推断生产 |
| RET | `retired-css-selectors.json`与当前源码；退役5个展示同步island、相关死库/测试及旧概览CSS，保留业务命令控制器 |

状态“通过”仅用于表列证据足以满足本次改动的对应行为；组合由服务、HTTP、浏览器分别承担，未将单一短fixture当作所有业务组合。已记录偏差保留原目标与原因。

最终映射为 **91项通过、4项已记录偏差（V05/P02/P05/P07）、1项上线前门槛（P06）、1项上线后测量（P08）**。97个ID无遗漏、无重复；没有剩余“待最终日志”。

## 逐项结果

### 数据与信息语义

| ID | 状态 | 证据与边界 |
|---|---|---|
| D01 | 通过 | U1逾期/有效补交与U2真实源日历一致性测试通过；BH实际看到补交9/7常显，FC当前学生截图同时保留补交与完整事项入口。 |
| D02 | 通过 | U1 `test_d02_past_class_is_not_completed_task`、`test_d06_does_not_claim_today_completed_from_deadline`与教师关闭历史测试；U2 日历源一致性测试。过去课次为 history/past，`is_completed=false`。 |
| D03 | 通过 | U1 `test_d03_lesson_does_not_become_submission_deadline`直接使用 9/10 课程，断言无 due/effective_due、不计硬截止、时间文案不含“截止”。 |
| D04 | 通过 | U1 `test_d04_natural_week_differs_from_rolling_seven_days`与`test_d04_aware_timestamp_converts_to_shanghai`覆盖周日/周一、Z/+08:00/仅日期。 |
| D05 | 通过 | U1无日期无哨兵测试通过；BH完整46项及本人待办6项可达（fixture含未设截止），新时间展示直接使用后端标签。 |
| D06 | 通过 | U1 `test_d06_does_not_claim_today_completed_from_deadline`断言新投影不提供虚构今日完成指标，已完成项归历史。 |
| D07 | 通过 | U1 `test_d07_d21_dedup_before_limit_and_stable_tie_break`以同名不同 ID、重复同源和倒序输入断言去重与稳定键。 |
| D08 | 通过 | U2 146项和10,246项keyset末页无漏/重；BW首页测试实查API总数、20项一页、第2页重开一致、空结果清除恢复。 |
| D09 | 通过 | UC拒绝推断任务session归属；BW selected lesson materials测试切课程首页后任务预览标题集合不变，课堂任务范围独立于所选课次。 |
| D10 | 通过 | UI的课堂退役断言、BE classroom两角色与FC最终图均无旧活跃仪表盘；新任务表示状态和N/M，不计算虚构100%活跃。 |
| D11 | 通过 | 已退役材料混合进度模型/显示，TC范围及UM只读权限测试通过。当前目录/选择仅由原材料控制器处理，不再输入任何学习进度展示计算；BW材料浏览不改变任务。 |
| D12 | 通过 | BK完整路由同快照校核：课程、profile、best_course、本人排名均2.3，阶段29%明确独立；首页与课堂SSR统一保留一位小数。 |
| D13 | 通过 | 阅读与掌握未混同：U1本人阅读记录才能续读；新课次材料仅展示名称/范围，旧“材料学习进度”仪表盘退役；保留的修为说明通过统一解释入口呈现计量含义。 |
| D14 | 通过 | U1 `test_d14_yesterdays_streak_does_not_claim_activity_today`分别断言 active_today true/false 文案；无记录时不声称今天已学习。 |
| D15 | 通过 | BK精确秒级普通/补交/个人重交边界，BT六阶段真实Chrome同时核对首页、课堂、全量和API；M保持、N及动作按有效窗口同步。 |
| D16 | 通过 | U1 `test_d16_date_only_lesson_is_never_in_progress`断言今日仅节次不进行中；U2 日期课次截止字段为空。 |
| D17 | 通过 | BK两个真实登录学生的课堂、普通作业、考试HTML及预加载JSON均无未揭示分数/评语；本人结果揭示后恢复，另一人仍保密。读取小组可见性失败503，私人试炼/待办负例通过。 |
| D18 | 通过 | BK缺失及dirty快照沿真实源重算16.4；故障503、课堂明确不可用而非0，移除故障可恢复。BW材料503常显、实际重试成功并打开原查看器；不把HTTP测试说成全组合浏览器测试。 |
| D19 | 通过 | U1硬截止优先于投票的固定时钟测试通过；排序函数对完整授权集合重算，已完成/不可行动项不占焦点；动态浏览器窗口覆盖另列D22。 |
| D20 | 通过 | U1 `test_d20_today_lesson_beats_unlimited_late_and_undated_high_priority`同时覆盖三类来源，今日课程优先且总数保留。 |
| D21 | 通过 | U1 稳定去重、有限/无限补交与返回窗口测试通过；TC `prioritizes the effective deadline across ordinary, supplement and personal resubmission work`补充课堂排序代码证据。 |
| D22 | 通过 | BT真实Chrome六阶段：课堂10:00 N2→10:05:01补交N2→10:10:01个人重交关闭N1→10:15:01补交关闭N0；首页待处理32→32→31→30（另有30条未来课程），首页M42/课堂M10及全量目标2条不变，每页仅一次document导航。20分钟快进+visibility恢复通过；BK另测精确边界秒、未发布→发布及既有starts_at规则，无逐行新增轮询。 |

### 首页路径

| ID | 状态 | 证据与边界 |
|---|---|---|
| H01 | 通过 | FN最终图：学生1366/390首课堂入口bottom740.5/737.0；教师742.3/756.1，均在指定首屏内，手机底栏top783；主行动同时可见。FC较早时刻学生390为761.0，同样达标。 |
| H02 | 通过 | BH搜索空结果→清除恢复；BE课堂两角色实际进入，当前dashboard.js保留原学期/状态搜索过滤与卡行直达。未修改的各个筛选取值不重复重演，保留原控制器合同。 |
| H03 | 通过 | U2完整过滤/授权/游标，BH远近来源和类型过滤、BW第一页20项/第二页恢复/范围总数通过；面板一次打开，条目href直达原详情，没有额外导航层。 |
| H04 | 通过 | U1/TD验证旧编辑器全部原值桥接，U2/PG本人私有；BE教师待办新增保存、默认不关联课堂通过，BH完成/撤回41→40→41。编辑/删除/提醒沿用未替换的原保存控制器，学生不可改课堂规则未移除。 |
| H05 | 通过 | BH实际订阅GET载入本人feed、单Dialog和返回焦点通过；订阅复制/打开/重置/教师提醒按钮继续使用原控制器流程。验收为能力可达与不绕过原流程，未执行外部日历订阅或真实邮件发送。 |
| H06 | 通过 | U1本人最近阅读及多材料绑定真实SQL测试通过；TC、BW真实viewer200验证课堂归因链接合同；新续读不使用课程更新时间代替本人记录，旧查看器未改。 |
| H07 | 通过 | U1/U2多课堂交集数据测试，BH实际搜索无匹配→完整事项0/日历空→清除恢复；前端将同一可见课堂ID集合传到两面板。浏览器样本仅单课堂，交叉集合由服务测试覆盖。 |
| H08 | 通过 | U1/PG教师工作份数与归属通过，BH待批改/审核信号可见；BE teacher-three-domains四项实际进入教学/教务/材料域、切换、旧链接重定向及手机角色隔离。监考数据沿用本人授权源。 |
| H09 | 通过 | BH实际核对学习路径/错题本/成绩单/成就/职业入口可见；模板href沿用原目标页，重复说明退役；未重演与此次移动无关的目标页内部流程。 |

### 课堂路径

| ID | 状态 | 证据与边界 |
|---|---|---|
| C01 | 通过 | FC两角色1366/390最终图：课次在上，任务区top约390.9/412.7（学生）、390.9/488.2（教师）；材料入口通过首层导航/区块可达，无修为墙阻挡。 |
| C02 | 通过 | TC首页0/课次真实ID/考试无材料请求、UM绑定首页合同通过；BW实际选择首页并核验session_id=0、generate_blurbs=false；普通课次/教务节点沿用明确entry_type分支及原上/下/全部导航。 |
| C03 | 通过 | 当前材料空态明确区分未配/教务考试，全部课堂材料按钮不依赖当前材料数量；UM空/旧主绑定与BW失败后恢复验证读取边界，学生无教师绑定管理动作。 |
| C04 | 通过 | TC N=0仍保留M及空预览测试通过，UI和BW验证全部任务使用单份完整原列表；功能不依赖历史条数，未将2条单测写成12条浏览器fixture。 |
| C05 | 通过 | TC使用7条/预览4条/溢出3条，新增有限截止与个人重交窗口2项测试；溢出按钮明确设置urgent并清搜索，和预览共用taskIsUrgent。BW最终实际紧急筛选只显示24小时内有限有效截止、排除无期任务。 |
| C06 | 通过 | U1真实补交/重交窗口与TC所有状态/动作通过；UI原详情/上传/批改动作保留，BW实际从全量任务到assignment详情再返回。无刷新边界另按D22保留覆盖限制。 |
| C07 | 通过 | BUS普通真实提交与教师回读通过；补充5264字要求首尾可读、100分评分要求和上传限制保留、补交80分封顶常显，实际上传qa-long.txt并提交200，刷新读到答案与附件。小组结果/窗口由BK联合验证，不冒称所有复杂组合都做过端到端提交。 |
| C08 | 通过 | BW材料失败恢复后打开真实viewer200，BC/BW确认原DOM不重建；原目录/README/渲染/文档控制器继续使用同一实例，详情返回不会丢目录对象。跨目录代表性点击未逐个重演。 |
| C09 | 通过 | BUS实际单选、详情返回、全选/半选、取消及计数一致，真实ZIP200；390px全选可达，超限项前端常显拒绝且不发下载请求，直接POST仍403。仅隔离服务启用260字节测试上限，随后恢复。 |
| C10 | 通过 | BQ exerciseClassroomActions实际开关原修为/成员详情；TC试炼/批改状态与U1本人试炼有效窗口保持，原阶段生成命令不被预览调用，UM只读预览无AI生成。 |
| C11 | 通过 | MAT真实绑定/解绑7/7、受控生成界面3/3、既有生成服务54/54。课次列表只读；AI/lessonDoc入口互斥、排队轮询、失败后可重试。生成POST全部拦截，无真实AI或队列写入；真实PG多进程生成互斥未在此轮压测。 |
| C12 | 通过 | BUS真实成绩用途保存回读并还原、期末材料教师深链、结课仅处理选中1项/跳过项仍存；BE教学/教务/材料三域及材料管理通过，BC分组交接保留筛选。其余原命令入口保留，相关closeout/retake服务在PY146中回归；代表性操作不是全部旧业务笛卡尔积。 |
| C13 | 通过 | BE classroom教师实际发送消息且页面恰有1条，WebSocket online；BC讨论/资源草稿切换，UI四活动原DOM/命令模块保留。原私信/协作/互动业务控制器未因收纳重建。 |
| C14 | 通过 | BUS对真实历史消息引用，实际上传合成图片但未发送；任务/资源/讨论往返后草稿、引用、待发附件及历史滚动均保留，附件可取消。BE另有真实消息发送；最终1366/390实测发送工具行在视口且命中正确，活动面板overflow visible、无额外内层滚动，截图已归档。 |
| C15 | 通过 | BQ 3/3实际验证二维码发布编辑、放大下载、键盘及无权拒绝，同时exerciseClassroomActions操作资料/统计/修为入口；原考勤/同步异常控制器保留，未新做真实教务同步。 |
| C16 | 通过 | BUS四权重合计100，真实预览/保存200，刷新回读一致；合成预警POST已处理200，刷新移出未处理列表；考试名册状态读取完成。UC/U1区分任务项数、待批改份数及成员数。 |
| C17 | 通过 | BUS材料单选→真实详情→返回保留选择；成员按学号过滤→真实iframe详情→关闭保留筛选、滚动、焦点。MAT单焦点所有者、嵌套确认暂停底层、Tab/Escape/背景inert/返回均实际验证，原材料DOM没有复制。 |
| C18 | 通过 | BK系统生成前缀+排课元数据+教师真实正文混合fixture：只移除可核对的连续初始占位，摘要保留教师正文，完整detail_content逐字不变；教师自写“上课地点”不被关键词整段删除。 |

### 权限与兼容

| ID | 状态 | 证据与边界 |
|---|---|---|
| R01 | 通过 | BE classroom实际点击材料/任务/活动锚点并断言在viewport，BW全部课次可打开；原hash分派与对应ID保留，不把隐藏原DOM当可见目标。 |
| R02 | 通过 | UI保留四活动ID，BE讨论实际可发送、BC资源切换；原classroom_page hash解析按活动ID显露相应tab，未用隐藏DOM作定位目标。 |
| R03 | 通过 | BUS真实教师deep link自动打开exam_paper；学生同链接不出现生成按钮且不打开教师面板，沿原权限处理。 |
| R04 | 通过 | TC与BW真实viewer200验证URL归因；本附录第13节复核首页绑定0、阅读URL省略session并归None，正数课次保留。旧阅读记录服务未改，不声明做过新的数据库上报实验。 |
| R05 | 通过 | BW assignment详情→浏览器Back恢复全部任务/P03筛选/同一DOM，BH完整事项关闭重开保留第2页，BC分组返回保留筛选；原材料目录实例及课次状态保留。 |
| R06 | 通过 | U2真实主班/合班、inactive403、撤销成员后游标/日历不可见；BE学生实际无关课堂直链拒绝。课堂原成员校验继续复用主/合班规则，不简化为单class_id。 |
| R07 | 通过 | BE非任课教师课堂直链被拒绝；U2/PG新API授权课堂发现、foreign过滤与本人私有事项通过，前端隐藏不作鉴权。 |
| R08 | 通过 | BK两学生SSR/预加载/普通和考试详情/API联合负例及揭示正例通过；本人待办与私人试炼不互见。分数、反馈、扣分前分与扣分字段在序列化前过滤，失败明确503。 |
| R09 | 通过 | UM权限拒绝发生在材料读取/生成之前，TC禁止javascript URL，BE学生不能进入教师材料管理；预览整数关联ID与原文件查看/下载访问校验保留，未扩大路径权限。 |
| R10 | 通过 | U1 `test_web_projection_is_opt_in_and_miniapp_context_default_is_unchanged`、U2 `test_existing_home_envelope_values_override_and_agenda_cap_are_unchanged`逐字段检查两角色旧外层、中文任务桶计数与40条agenda。此为固定旧契约比对，非生产小程序实机回归。 |
| R11 | 通过 | UI显式隔离11/11通过，断言新island存在而重复boards/sync不存在、原业务模块仍在；FC32组稳定图0 JS错误，旧大概览DOM已退役，非CSS隐藏两套首屏。 |
| R12 | 通过 | BW任务面板20次开关：原DOM对象相同、单源节点、document监听不增、WebSocket数不增、草稿保留、无pageerror；代码effect清理与单实例复用检查。该实测计数不冒称覆盖所有window监听/计时器类型。 |

### 布局与密度

| ID | 状态 | 证据与边界 |
|---|---|---|
| V01 | 通过 | RET当前首页模板与BH实际观察一致，FC最终两角色图已归档：仅一份重点清单，六格/脉搏/重复侧栏退役。 |
| V02 | 通过 | UI与BE显式断言旧task/material/progress展示sync退役，FC最终课堂图无三类旧大仪表盘；原详情能力保留。 |
| V03 | 通过 | U1首页最多3、TC课堂默认4/溢出/空态、材料slice(0,3)通过；BH首页完整46、BW任务M与分页可达，FC不补空卡。 |
| V04 | 通过 | 第10节逐类记录四图旧元素去向，RET保留退役CSS清单；FC现有单课堂同初始数据库64图支持重复呈现核销。不将32课次当33次教学课，也不声称完整F01考试组合。 |
| V05 | 已记录偏差 | FC同初始数据：学生桌面−40.7%、手机−73.6%，教师手机−64.9%；教师桌面382→415.6px（+8.8%）未达−40%。保留三条真实行动与可读字号；首课堂入口约1210→742.3px。F02完整前后高度未补测，不以改变边界或缩字凑目标。 |
| V06 | 通过 | 当前新课堂/首页作用域样式显式退役逐行重阴影和多层旧表面；FC两角色桌面/手机图复核，主层按行和少量分隔面呈现，原业务二级表面独立。 |
| V07 | 通过 | 作用域样式cw-workspace-flow/cw-primary-grid为28px间距，首页章间24/28、组内8–16；FC已加载字体图与BH手工对齐检查一致。局部紧凑按钮间距按控件组处理，不作为章节间距。 |
| V08 | 通过 | AX两角色两页长中文标题20组，200%文字与独立容器复核；修正容器换行及任务状态窄列，未发现单字竖排、关键重叠或整页横溢，完整正文沿原详情可读。 |
| V09 | 通过 | AX补齐1920×1080、1440×980、768×1024、320×844及原390/1366/1024样本，320×245为400%等效重排；68组核心数值/布局与56PNG，普通内容无整页横溢。日历允许局部二维滚动。 |
| V10 | 通过 | AX viewport独立的320/480/640/800/960内容容器实测；新首页/课堂改按容器断点重排，640px容器不沿用1440px视口的多列压缩。 |
| V11 | 通过 | 当前模板单源DOM与预览插槽按课次→任务→材料→修为→活动，移动/桌面同结构；UI与BW检查单源DOM、20次不复制。视觉换列不创建另一套重复ID或操作DOM。 |
| V12 | 通过 | 末轮修复将课堂N徽章限定actionableCount>0；N=0仍保留全部任务M。当前主行动模板/作用域样式每区不重复实心主按钮，状态来自canonical投影；FN最终两角色图复核，没有用无意义0卡补位。 |
| V13 | 通过 | 首页仅绝对时间标签和单一边界刷新；课堂保留原考试时钟，TC含assignment-clock-runtime3项测试、可见性校准代码。普通远期列表不启用逐行秒级计时。 |
| V14 | 通过 | BE待办短屏footer/全部关闭控件，BQ手机/横屏Dialog边界，BH390日历主周与切周、BC320/390菜单实际通过；新Dialog内容区单滚动及body锁定保持原外壳合同。 |
| V15 | 通过 | AX两个校区同名B310的8组真实SSR前台样本，首层保留校区短名，不靠进入详情辨认；测试后合成库已恢复。最终课次图修正fixture星期偏移，9/10明确周四。 |

### 无障碍与说明卡

| ID | 状态 | 证据与边界 |
|---|---|---|
| A01 | 通过 | AX最终稳定16组：两角色×两页×首层/面板×浅色/OS暗偏好，保守渐变数值校核，首页最小4.572、课堂4.542，0低对比。课堂面板补验先等8张真实卡与动画结束，学生62/教师165文本；展开菜单实际像素不足及旧成绩用途提示已加深并复验。应用固定浅色，OS暗偏好仍为浅色，独立暗色主题N/A；不是全系统WCAG认证。 |
| A02 | 通过 | AX实际200%文字缩放与320 CSS px/320×245等效400%重排，含长标题、完整事项/任务/材料面板和短屏关闭；关键内容可达，无整页双向滚动。 |
| A03 | 通过 | AX四主模态共280次Tab、ShiftTab/Escape，首层逐控件几何命中；MAT单焦点栈通过。BUS最后复验所有单选框具材料名、1366/390发送工具行可见可命中；AI3个角色/页面场景真实背景点击、上层模态优先、Escape与关闭回焦点通过。 |
| A04 | 通过 | AX紧凑按钮、行、图标实际尺寸及间距审计，无未处理目标失败。材料单项13px为未改尺寸/appearance的原生input，按WCAG user-agent例外，不能声称24px或spacing例外；实际名称已补并逐行验证。全选测label整体32px、粗指针44px。 |
| A05 | 通过 | 最后修复增加课堂粗指针规则，BW在390触屏上下文确认pointer:coarse，并实测全部任务/材料、说明、课次前后等指定新主要控件宽高≥44px；首页作用域44px规则与Dialog关闭入口保留。未把该项目目标误写成WCAG AA统一44px。 |
| A06 | 通过 | UE 7项浏览器实际通过，覆盖focus/Enter/hover/Escape、移出触发器仍能阅读卡内链接、退出卡恢复文档顺序；共享实现没有阅读自动消失计时。 |
| A07 | 通过 | 末轮以显式WorkspaceExplanation按钮取代仅在h2挂说明，带标签、data-explain-toggle；BW实际打开课堂任务说明并Escape关闭，UE键盘进入卡内链接/返回的7项交互通过。 |
| A08 | 通过 | UE模板/运行时代码合同2000/300/650ms，实际长按出现、移动30px后750ms仍未打开；普通标注控件Tab/点击未被吞。延迟常量通过代码断言，不声称进行了毫秒级仪器测量。 |
| A09 | 通过 | AX四主模态280次Tab、背景锁定、Escape与回焦点；MAT三种旧材料模态单栈/嵌套确认。AI3场景验证浮动工具保持非模态、真实模态覆盖并独占Escape，完全卸载后恢复AI与关闭回焦点。Radix恢复aria-hidden=false可表现为移除属性，不能误报未恢复。 |
| A10 | 通过 | UE keyboard与ordinary annotated controls实际通过：focus显示不抢焦点、Enter可进入、Tab离开到文档下一项、Escape回触发器；保留非模态语义且未套模态陷阱。 |
| A11 | 通过 | UE实际唯一popover断言，运行时媒体只在打开后赋src、lazy创建；页面prefers-reduced-motion明确关闭非必要过渡。代码合同+代表性交互验证，不把无媒体fixture说成做过每个媒体资源抓包。 |
| A12 | 通过 | BK真实SSR保持补交约束、组内未揭示、生成中/失败及修为不可用；BUS实际提交结果、下载限制拒绝常显，MAT生成POST503错误可见且可重试；BW材料未配/503重试直接呈现，未将这些必要状态收进hover。 |

### 性能与并发

| ID | 状态 | 证据与边界 |
|---|---|---|
| P01 | 通过 | U2两新端点只读trace且不调用整页builder，UM预览generate_blurbs=false不生成/不持久化，BW503重试检查同参数，PG203表哈希不变；原整页副作用未复制到新二级路径。 |
| P02 | 已记录偏差 | SQ新workspace读取1/6/30课堂SQL固定学生11/教师10，日历和事项复用同一授权源，无新增逐课堂N+1。PF补完整路由trace；旧共享dashboard context重复计算仍保留，本轮未扩大到重写所有消费者。一次遍历稳定排序key复用有1000来源工作量测试。 |
| P03 | 通过 | PF 41次独立trace记录F02/F04整页/JSON原始及gzip字节、SSR元素、SQL数/DB-API耗时；CP/HP保留浏览器DOM/脚本时长/LoAF/真实交互。F02学生首页gzip50006→36719字节、SSR元素2331→514；F04教师课堂gzip68871→70105也如实保留。5个展示同步island及观察器删除，原业务时钟保留，不声称所有interval都删除。 |
| P04 | 通过 | 按第14节明确记录的预算调整验收：SSR及完整事项默认20、API单页/候选≤100；keyset10,246项末页可达且total准确。去重键O(N)，日历及原课堂业务DOM保留完整合同，不声称总内存100。PG60并发<1s和载荷实测支持当前边界，不外推生产容量。 |
| P05 | 已记录偏差 | PF修正时钟后F02/F04冷/热320次，首次越线3格各30/版本复验180次；500次均200、同seed SHA、服务/SSR实际10:00、外部HTTP0。仅F04学生首页hot持续62.48→69.83ms（+7.34ms/+11.8%），超过10%目标；保留偏差，不继续换样本。其余15格达到样本门槛或初次越线未持续。旧740比较已撤回。 |
| P06 | 上线前门槛 | PG16.14本地固定pool8，10/30/60各3轮共300次全200，连接全部归还、waiting0、203表哈希不变。真实staging固定资源与已有后台队列未执行，仍是A类发布门槛；本次本地交付不包含部署许可。 |
| P07 | 已记录偏差 | CP课堂8样本CLS均≤0.1；慢教师LCP2500/2496/2516ms，学生2460/2312/2392ms，慢首次任务面板184–552ms；正常首次打开学生72/教师56ms，教师关闭单次352ms保留。HP首页慢配置6样本LCP2240–2468ms、CLS≤0.000337，均达标；最大EventTiming有440/488/424ms的3个超标样本。实验室不冒充线上INP p75。 |
| P08 | 上线后测量 | 未部署、未采真实用户数据；手机/桌面p75 LCP/INP/CLS全部未确认。 |
| P09 | 通过 | BW20次任务面板切换document监听/WS不增、DOM单实例、草稿不丢；一个后退恢复及材料课次切换通过，代码检查隐藏面板未引入重复init或逐行轮询。非所有活动组合的笛卡尔积实验。 |

## 设计取舍与原能力去向

| 原呈现 | 最终去向 |
|---|---|
| 长篇问候、统计六格、下一步、课程脉搏、深色待办墙 | 简短日期问候，一份最多3项的主行动，课堂列表；完整事项/日历共用二级面板，SSR/默认每页20、API最多100 |
| 活跃率、重复状态统计、任务横向挤压仪表盘 | 首层4项任务预览、准确待处理N/完整M、真实补交约束和紧急溢出；完整列表单DOM进入面板 |
| 课次/README/已选混合材料进度仪表盘 | 所选课次最多3项材料；完整课次/课堂材料分清范围；目录、README、渲染、文档、原下载权限仍可达 |
| 展开式修为墙 | 一行课程修为与原详情，真实阶段/排名留二级；不改评分规则，不把阅读说成掌握 |
| 自动生成的重复课次元信息 | 首层短摘要保留校区、节次和真实正文，完整正文可读；不能确认来源的文字不删 |
| 辅助说明与管理入口 | 既有共享说明卡及角色工具；hover2000/focus300/长按650ms合同保留；失败、权限、未配、截止、补交约束和未揭示状态仍常显 |

课堂任务没有可靠session_id，切课次只切材料，不重新归属任务。材料列表首页绑定使用session_id=0；原阅读器首页URL省略session_id，保留class_offering_id，正数课次带真实ID。原阅读进度按课堂/学生/材料归因，未另造首页阅读桶；原query/hash保留。

完整任务与材料业务DOM移动而不复制，原提交、批改、分组、结课、重修、期末材料及活动控制器保留。后台/权限/数据库变化仅修复本次审查确认的问题：未揭示结果在序列化前过滤、截止整秒补交空档、读取失败明确不可用、材料只读预览不生成简介。

## 性能偏差与发布边界

V05教师桌面区域没有达到减少40%的相对目标，保留三条真实跨来源行动，不缩小字号或裁切状态凑比例。H01首课堂可见的绝对目标已满足。P02既有共享context仍有历史重复计算；新增事项/日历使用同一源，未扩展为全面重构所有消费者。

P04预算明确为每页/候选最多100、默认及SSR20，授权去重键O(N)、精确total与原日历保留全量。10,246项keyset末页无漏/重和1/6/30课堂固定查询数支持该边界；没有声称总内存仅100项。原课堂完整业务DOM只保留一份，未为布局改版再替换全部任务控制器。

P07保留每个慢网络/CPU样本和超标交互；实验室LCP/CLS/EventTiming不是线上INP或p75。P05以修正后匹配时钟报告为准：早期datetime类mock造成current时间00:00与baseline10:00不等，旧740条比较已撤回，原始文件保留以便追溯。

P06仍须在获授权的staging固定资源下验证真实后台队列/长时并发；本轮只运行合成副本及本地PG读取，未启动真实邮件、教务、AI或生产worker。PG测试导入203表schema并跳过既有首次初始化迁移错误（text/CURRENT_TIMESTAMP），不宣称全新PG初始化通过。P08手机/桌面真实用户p75只能上线后测量。

## 最终执行登记

| 执行 | 实际结果 | 证据 |
|---|---|---|
| Python联合 | 11模块146/146、无跳过；时间断言加强后BK8模块114/114再过；末次模板改动后认证SSR11/11再过 | `continuation/final-closure/continuation-python.log`、BK日志、`closure-authenticated.log` |
| 前端 | 类型检查通过，Vitest157项/29文件通过，CSS+Vite生产构建通过，git diff --check通过 | `continuation/final-closure/`下typecheck、vitest、build、diffcheck日志 |
| 受影响浏览器回归 | 六份spec23/23、4分钟；任务/材料、待办、三管理域、统一说明及触屏主要控件 | `closure-browser.log`逐条带名称 |
| 业务操作 | 11个不同流程通过；最后补验3/3、33秒，含5264字要求和真实补交附件、选择框名称、讨论工具行1366/390 | BUS `results.json`逐条指向最后通过日志，不重复累计重跑 |
| AI浮动工具 | 学生课堂、教师首页、教师课堂3/3，背景真实可操作，上层真模态Escape不误关AI，完全卸载后工具内Escape/关闭回触发器，0页面错误 | `continuation/final-closure/ai-results.json`、`closure-ai.log`、`ai-probe.cjs` |
| 材料/生成 | 实际绑定7/7、受控生成UI3/3、既有生成服务54/54；真实AI调用0 | MAT独立报告及JSON |
| 布局/无障碍 | 核心68、长标题20、两校区8、最终稳定数值16、主模态Tab280、早期加载入口9等分组记录；不将不同组相加成一个测试数 | AX报告与分组JSON/PNG |
| 时间/性能 | BT六阶段/12PNG；PF有效500配对请求+41独立trace；CP课堂8/HP首页6慢设备及对照样本 | 各独立报告保留时钟、环境、版本、原始偏差 |

最后两项小修（成绩用途提示对比色、课堂AI延迟入口禁用）只涉及模板和直接加载的静态CSS/JS，无Vite模块改动；前者有四组完整面板数值复验，后者有3场景真实首次点击与认证SSR复验。历史性能仍对应各自源码哈希，不虚构对所有历史样本再跑一遍。旧ARIA探针在真模态动画结束前检查，且把aria-hidden属性移除误判为失败；原始诊断保留，最终测试等待DOM完全卸载，不吞Escape或绕过焦点层级。

不同层次及重复回归不相加成含混“总测试数”。

## 交付与环境收尾

本轮未提交、推送或部署；用户原有图片及证书目录不纳入本次改动。最终截图和脱敏证据留在artifacts。已按进程命令行核实并关闭本任务QA/受控时钟服务，8131/8132/8133/8135/8136/8137/8138/8139及隔离PG6543均无监听；未停止其他服务。

自动审批审查此前两次拒绝删除 `.codex-temp/home-classroom-redesign-20260905`，工具仅返回 `blocked by policy`，未提供更具体理由。未换工具绕过限制，临时副本保留；最终体积与残留检查写入cleanup记录。其他临时目录及坚果云数据保持原样。

最终残留检查：本任务临时目录 9546 个文件、约 2.25 GiB，删除未重试。详见[cleanup-final.json](../artifacts/home-classroom-redesign-2026-09-05/cleanup-final.json)。
