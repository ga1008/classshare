# 三类任务与多模态调度生产发布记录

2026-09-07 18:15（北京时间）完成最终生产部署，发布标识为 `20260907-181359-4545404e64e6`。业务源码提交为 `a55ce2e14e50b40a81907ebc5b1fecac09598180`，已整合当时最新主分支 `a48ab05c`。后续发布记录提交只补充文档，不改变运行代码。完整脱敏证据见 [发布报告](assessment-routing-production-release-2026-09-07.json)。

## 已启用的业务规则

| 情形 | 图片处理 |
| --- | --- |
| 平时作业 | 豆包 Pro，low 思考 |
| 期中测验、期末测验 | 豆包 Pro，high 思考 |
| 历史课堂任务尚未确认分类 | 保持原数据；需要图片时保守使用 Pro high |
| 即时对话、私信、博客等边缘识图 | 豆包 Lite，low 思考 |
| 纯文字任务 | 继续 DeepSeek，关闭向豆包自动溢出 |

Pro 为 `doubao-seed-2-1-pro-260628`，Lite 为 `doubao-seed-2-0-lite-260428`。模型能力不因学生作答或图片中的指令改变。教师端、学生端、成绩统计、正式成绩材料和公布快照使用同一分类及有效成绩规则；教师需要显式确认旧任务分类，不按标题或有没有试卷自动猜测。原成绩公式及已保存材料保留。

全局并发 6、DS 4、豆包 2，high 子并发 1。每个逻辑任务最多两次可能计费的生成、总共三次 HTTP 尝试；自动升级复核受每日全站 10 次、每课堂 3 次的持久额度约束。现有 API key 和非本轮配置逐字保留，默认评分链只启用已确定的 DS／豆包候选。

## 旧数据保护与实际迁移

上线前重新导出 PostgreSQL 一致性备份，完成两次独立恢复，并对 220 张旧表、469,779 行验证两次增量迁移与两次完整初始化。旧字段、主键、关联、结构及序列均无变化，迁移具有幂等性；没有使用时间戳或序列差异豁免。53 个迁移源文件的 SHA 与实际发布包绑定，不能用旧报告代替当前源码。

生产切换前确认旧批改内存队列 pending=0、持久 AI 队列 active=0、Agent 无活跃任务。先构建共享镜像，再停止 app、AI、mailer、blog-crawler、scheduler、agent-worker，取得停止写入后的最后一份 PG 备份，执行一次新版本 schema 初始化，最后恢复整套服务。158 个必需表齐全，失败索引和跳过步骤均为 0。没有恢复旧数据库覆盖生产新提交。

备份位于服务器受保护目录 `/var/backups/lanshare/assessment-20260907-084343`：

- 新演练 PG 备份：32,126,732 bytes，SHA `81947e0f9c04303c688069b2285149c20177623b64cf0cbe6f6d8e8317751088`。
- 停写后的最终 PG 备份：`cutover/db-cutover-20260907-181407.sql.gz`，31,407,168 bytes，gzip 完整性通过，SHA `628471df2623e234ec85f1343601fc84c29adf5c5d977944c6fe0f8180b864b1`。
- 附件与运行文件备份：2,768,803,773 bytes、18,949 个文件；排除运行中的 PG 数据目录，数据库另用一致性逻辑备份保护。
- 旧代码、配置切换前后副本及旧容器镜像回退标签均保留；最早的旧代码备份另存 `pre-feature-code-20260907-172632.tgz`，避免补发过程的常规备份轮换影响它。回退应保留新增 schema、分类审计与公布快照，不用旧库覆盖上线后的写入。

上线后使用强制只读事务核对 10 张核心表：历史任务、提交、附件登记、成绩版本、材料及相关关联的旧字段多集散列、主键散列、关联散列和行数全部一致。98 个任务未被擅自分类：46 个历史课堂任务仍待确认，52 个个人阶段任务保持独立。

13,342 条提交文件登记和 1,815 条有效材料登记均可找到文件。14 个历史样本的图片／Office／文本可打开，内容散列与上线前一致。没有将全量路径检查称为全部文件内容质量验证。

**已存在的文件异常：**3 条旧 DOC 登记的大小、散列与现有 915-byte RTF 文件不符，上线前已确认。本次保留这三条原登记、现文件和备份，未改写或冒称恢复了原始内容。

## 上线验收与边界

- 主站、AI 健康检查通过，真实容器使用同一新版本。公开登录页返回新 release；首次响应仅清理 HTTP cache，第二次不重复清理，不清除登录或答题存储。4 个相关公开 JS 文件与发布源码 SHA 一致。
- 生产主机和 app、AI、agent-worker 的 71 个公开 AI 参数逐项一致。新 AI 容器内的 77 项检查、14 个业务场景及 7 次 SDK MockTransport 请求通过，检查了实际 model、思考档、输出上限与预算。真实供应商网络和数据库访问均为 0。
- 32 项生产只读服务条件检查通过；实际样本验证旧有效分、缺交 0、未评分、正式任务与个人试炼分离。线上没有已公布、重批中或未揭晓样本，空结果不能作为这些状态的线上验收证据。
- 合并最新主分支后，TypeScript 与生产构建通过；208 项前端测试、21 项浏览器组件测试、51 项 AI 路由与健康测试、103 项材料与成绩相关测试、18 项部署门禁测试通过。这些分组可能包含重叠用例，不合计成独立测试总数。之前本地 469 项组合回归及合成数据库师生真实路由 E2E 记录仍保留。
- 本轮另外修复扫描材料视觉导入最多 8 页时静默漏页的问题：实际视觉输入不完整时在调用前返回明确错误，已有完整文字解析和本地正式成绩 Excel 分支保留。

收尾日志发现原有学情查询缺少 PostgreSQL 分组字段；补齐后，大样本又暴露多路历史关联的行数膨胀和 10 秒超时。最终改为行为、材料、作业、证书分别汇总再关联，纠正时长与提问数的重复累加。同时移除学生 AI 上下文中的原始分数均值，直接复用共享可见成绩投影：未揭晓、失效成员或被退回的分数不进入均分，揭晓后使用综合分，保留真实零分及重批前有效分。该修复不改写旧成绩或旧画像。

此项新增 SQLite、原生 PG 与共享投影合同共 14 项通过。隔离的 5,199 条行为样本完整查询为 46.77ms；最终线上只读大样本完整 3 条 SQL 为 72.55ms。离线与线上样本的其他计数不同，不将其解释为新增提交或同一学生对照。最终观察恢复后 109.9 秒，9 个容器全部运行、6 个配置健康检查的容器全部健康，没有新的 PG 分组／取消错误或应用异常。切换期间 nginx 的 8 次拒连均在服务恢复之前，恢复后无此类错误。

没有向线上创建测试课堂、答卷或成绩，没有自动重批历史作业，也没有额外付费模型调用。未取得线上师生已登录浏览器会话，因此公布／撤回完整交互使用之前的本地真实路由 E2E 证据；这不能描述成线上人工验收。小程序已完成源码对应、类型检查和构建，本次没有正式上传小程序。

本地专用 PG 集群已停止。部分本地测试产物清理被自动审批以 `blocked by policy` 拒绝，保留原地，未改用其他方法绕过；备份不在同步仓库中。个人部署脚本仍按既有规则忽略，不含入 Git 或交付归档；通用迁移校验与合同测试正常纳入版本控制。
