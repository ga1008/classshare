# 背景与外观菜单修复发布

- 外观中的配色、明暗、玻璃效果及背景选择复用既有 LQ Selection 玻璃菜单。原生 select 继续保存唯一值，保留键盘、焦点、保存冲突与重试处理；不再弹出系统白底选项框。
- 首页、课堂等共享原图背景，透明度为 1，仅进行 24px 高斯模糊，外扩 48px 消除边缘漏白。移除全页白色遮罩；body 建立独立层叠上下文，避免负层级背景被页面底色遮住。
- 背景切换和关闭保留上一帧并用 320ms 淡出；菜单沿用统一进出动画及减少动态效果设置。

## 定向验证

- 6 项外观菜单组件用例、14 项 token 检查、TypeScript 和 LQ lint 通过。
- 学生桌面与教师触屏的首页、课堂共 4 次访问均返回 200，无脚本错误和横向溢出；背景样式与实际像素对照通过。
- 3 处已有外观入口完成打开、选择、保存、Escape 退出。教师课堂原模板没有外观入口，临时脚本在等待该入口时超时；背景验证已先完成，未将此脚本记为全项通过，未新增角色入口。
- 背景关闭的过渡帧、320ms 淡出及动画后清理通过。未扩展为全站测试；触屏为 Chromium 模拟。

## 发布

- 产品提交：`680c31a6a6347e76696ab03f231edcc1c44837d1`。
- 生产版本：`20260923-194250-1c5b833528d8`。
- 资源图：`ef53289828a6d932bf145d2e67d5f8078dd8728b4272bb24992179d96005b637`。
- PostgreSQL 部署门禁、停写备份及发布检查通过；7 个变更生产源码与容器 SHA-256 一致，8 个公网资源一致，登录页返回 200。
- 8 个服务 running，6 个配置健康检查的服务 healthy，主服务与 AI health 为 ok。后台任务仍保留 719 条既有失败历史，11 类任务当前状态均 ok，stale=0；未清除历史记录。
- 代码备份：`/tmp/lanshare-deploy-backups/code-20260923-194303.tgz`；数据库备份：`/tmp/lanshare-deploy-backups/db-cutover-20260923-194303.sql.gz`。

证据：`.codex-temp/glass4-targeted/summary.json`、`glass4-deploy.log`、`glass4-postflight.json`、`glass4-services.json`。
