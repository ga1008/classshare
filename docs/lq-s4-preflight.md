# S4 分批实施预审

2026-09-21。依据最新执行计划及当前源码制定；S3本地工程出口已完成，最终图为 `de1603bb1755d603744b9d612d4b0808b676f53c07df6dce0f8e0d362ff4d743`。此文件是S4施工边界，不代表S4产品已经交付或通过。

## 前置与分工

S3的真实业务链、管理/成绩试点、布局、失败回退、层数与性能门禁已有实际出口证据，可以开始S4产品修改。正式签字、真实机型、生产发布与本地测试事故的数据恢复分别记录；浏览器能力模拟不能填写为实机结果。

| 批次 | 独占范围 | 主要依赖与出口 |
|---|---|---|
| A 共享壳 | 管理 layout/partial、学生 base_navbar/底导航、壳 adapter、统一页族开关 | 复用 S2/S3 单一 owner，准确路由与服务端权限投影；原节点、iframe、业务动作不重建；新旧分支可独立回退 |
| B 首页与日历 | 双首页及其 React workspace、agenda、semester_calendar、对应页 CSS | 依赖 A；同一日历和课表实例，待办真实增改删闭环、迟到请求与脏草稿、跨断点和高度/CLS；3D 内核留在 S6 |
| C 资料与消息 | Profile 正文分区、师生薄壳、真实消息入口及其 island/controller | 依赖 A；先抽共享消息正文再分工，避免两人修改同一模板；外观三字段沿用现有 CAS 队列，联系人/附件/权限/失败重试完整 |
| D 登录与状态 | 学生/教师登录、注册、过期与拒绝/错误页、centered 壳、登录场景 | 可与 B/C 并行；真实登录卡背景逐张对比，失败/超时恢复、无脚本与来源跳转；禁外发邮件与真实重置账户 |
| E 成长页 | 学习路径、成就、商城、成绩、错题本、反馈复盘 | 依赖 A；成绩页复用 S3，其他页面分别保留本人资源范围、零/空/未公布边界和原业务 owner |
| F 常规管理页 | 准确清单内的剩余列表页和三宏调用点 | 依赖 A；逐页登记筛选/编辑/删除/失败/角色门禁。复杂工作台正文、系统管理与编辑器归 S6，不因共享管理壳提前扩入 |

共享开关、路由 context、CSS 入口与静态构建由主任务统一拥有。页面包只编辑自己的模板/controller/CSS，不自行增加 query 或 localStorage 开启方式；生成资产由主任务统一构建，验收日志绑定实际 graph。

## 当前源码中的必要纠偏

- `/message-center` 实际重定向至 Profile 消息分区。迁移入口必须覆盖 `/profile?section=notifications/private` 与教师 `/manage/me/notifications/private`，保留 tab/contact/scope 与锚点；单改旧 standalone 模板不能完成消息迁移。
- Profile 目前的 section 白名单没有新外观分区。增加分区和双壳选择属于必要窄路由票，不重写鉴权或业务服务。偏好仍使用 `palette_key/appearance/glass`，现有 240ms 队列、逐字段冲突和显式恢复合同不能被计划中的简化描述覆盖。
- 双首页共享 React `CalendarHost`，会移动并归还原 calendar DOM；原 calendar controller 没有 destroy。禁止创建第二实例或通过删除/重建节点声称完成卸载。
- 学生 bottomnav 仍有旧 fixed 层和 body 补白。接入 Dock 时需一起协调 composer、安全区、软键盘与 contentRoot，避免重复补白；非受控文档 iframe 不接主题。
- `base_centered.html` 已抽出样式并缩为薄壳，不再按计划中的旧行号重复抽取。学生 Clear 卡仍需真实登录表单验收，S1 的 316 图材质试件不是登录业务验收。
- 管理页页内原 dialogs/controller 在 S3 仍保留。S4 复用壳不等于全页控件已经迁移；台账的 partial 范围、准确依赖和共享源审阅指纹须继续保留。
- 原 `dashboard-schedule.spec.ts` 的源码 test 声明数与计划旧计数不同，验收以实际收集和执行报告为准，不删测试凑数。

## S3 提供的基线与风险

- 第五图下学生首页 390×900、4 门课程、3 个可见焦点事项的文档高度为 2114px；初载 CLS 为 0.067390584，超过后续 0.05 目标。它是 S4 基线，S3 的只读成绩页验收没有替首页放行 CLS。
- 管理壳试点暴露了共享页头选择器、旧暗色中性面、原生更多与 body 弹窗交接问题；后续页族先列实际消费者与事件顺序，再接线，不能只替换类名。
- 渲染阻塞能力必须单独检测。缺少能力时，S3 保留 SSR 内联导航并由同一增强器继续操作；无脚本和模块失败另有退路。S4 新壳须继承此合同并稳定异步用户身份/消息内容的空间。
- 旧 LP 材料确认从已协调详情层打开时，必须作为其子层参与 inert、Esc、焦点与锁计数；独立旧 LP owner 的同步合同暂留。后续完整迁移再消除兼容分支。

## 每批最小验证与回退

1. 先固定合成身份和资源，核对已有测试、实际 controller、接口和所有状态；禁止两个 runner 共用单会话账号。
2. 保留当前 URL、权限过滤、表单归属、附件/草稿、请求队列、版本与通知语义；异步失败不能伪装成空列表或成功。
3. 按相关断点、触控、亮暗/off、六配色和强制颜色检查真实页面；CSS zoom、当前 WebKit 引擎与实体设备分别记录。
4. 对真实父子层检查键盘、取消、dirty veto、强制关闭、父销毁与迟到回调；20 次操作核对请求/监听/实例，不只比较 DOM 数量。
5. 同一正式资源图完成目标页、旧入口回归和关闭该页族开关的回退；共享源更新审阅指纹前，重新核对对应门禁。
6. 记录原失败、修复与复验，补动作台账/组件合同/验收记录。只在实测业务闭环完成后推进该包；不将源码存在或测试收集写成通过。

真实设备记录目前保持未测。后续批次规模按实际状态与共享依赖评估，不凭模板数量给固定完成天数。

## 第一轮实现与开关合同（2026-09-21，待整包验收）

`classroom_app/lq_migration.py` 的 `lq_family_enabled` 注册为 Jinja global，只读取服务端 `LANSHARE_LQ_FAMILIES`。值为精确、区分大小写的逗号列表：`manage-shell,navbar-shell,dashboard,calendar,profile,messages,centered,growth,manage-pages`；缺省、未知值、布尔值、`all`、通配符均不启用任何未明确列出的页族。列出键不等于该包已实现。查询参数、header和浏览器存储不能开启它；部署环境变更按正常流程重启进程。原 `LANSHARE_LQ_PILOT` 九路由合同独立不变。

当前接入 `manage-shell`、`navbar-shell`、`centered`。共享管理CSS与八个S3试点的正文兼容CSS分开，嵌入文档保留原高度协议。共享学生顶栏复用S3的同一Shell/反馈交接逻辑，成绩图表仍由自己的controller管理；新Dock保留真实SSR链接与服务端激活态，消息分区激活“消息”，没有旧body补白叠加。关掉S4页族时，如果S3独立开关仍开，九条原试点保持S3呈现；恢复全部旧呈现需同时关闭两类开关。

C0已将Profile拆成学生/教师薄壳与共享partial，固定模板选择、不动态拼用户输入；32份旧/新完整渲染语义一致、9项隔离测试通过，证据 `lq-s4-c0-*`。外观分区、消息正文控件与原异步竞争修复尚未在C0实施。真实个人中心总览canonical链接问题保留在后续接线票。

D第一包复用原HTML登录POST与同一认证编排，补就地失败SSR、账号/安全next保留、POST缓存头、共享safe-next拒反斜杠和控制字符；教师自注册维持403。35项隔离认证/恢复/凭证测试通过。首次设密与找回的完整无JS路径、一言样式迁移、真实会话过期模板入口仍待后续包，不将已有密码fallback扩大表述为整个B16通过。

首轮正式静态图为 `673c4cf078930a2397be7f4d713ebaec8e81e9341c2f5c25c5df13ee3f0d8d1f`，414文件，11,479,006字节；LQ原生入口gzip 17,309/18,432字节且源指纹一致。管理壳11项SSR及15项源码浏览器门禁、共享成绩壳抽取7项生命周期单测/5项SSR已通过；新图真实页面、认证、回退、视觉和层数还在验证。测试只使用新建合成运行时，不访问本地5432或生产数据。

## 第一包修复与正式图复验

第二图 `d9357134027c998b8dc88b325211523081b0d1e73194e0596826a44e35334671` 修复登录错误的透明软色底、材质切换瞬间前景配对，以及学生手机顶栏场景图造成的第三个持续模糊。第三图 `8e24e14ff3b353d2d53a9652ca6f0cba268ffe43847ec2d8347cacee9caaf2f2` 再修复旧学生首页96/112px宽度规则挤掉新外观面板选项文字；414文件、11,480,100字节。该视觉问题在axe零违规时仍存在，保留第二图截图，并新增选中值文字宽度和面板边界断言。

共享壳第一图真实14条路由、双尺寸、20轮原节点复挂、无JS/模块失败共6项通过；第三图双角色×双尺寸×六配色×明暗及320px窄屏5项通过。上述6项中的20轮只证明节点/草稿与模拟Dock键盘回退，监听、请求和性能另有独立门禁，不由节点计数代替。新增navbar SSR 11项通过；管理SSR 11项、C0 Profile 9项与页族开关3项的隔离结果均分别留存。

登录第二图完整20项纯真实模板浏览器、316原背景×明暗×双尺寸1264项实卡像素检查全部通过，最低5.23257；48组SSR/JS错误色对最低6.12294。20次材质切换420帧检查的是计算前景/填充/遮罩配对，像素测量在端点，不声称每帧独立PNG。实际认证第一图为19项通过后修正测试文案重跑1项通过；第二图另4项JS错误状态与真实来源链复验通过，不能记成同轮20/20。axe对图像背景的incomplete保留，反馈点另取实际PNG像素约6.14。状态失败图标由未登记circle-x改为circle-alert，6项隔离模板与1项定向浏览器通过。

这些结果关闭的是明确子范围。第一包的服务端flag关闭、新增壳性能/资源门禁仍在本地收口；C1外观与Profile正文、C2消息、D完整设密/找回以及B/E/F尚未验收。D纯模板与实际认证的详细原失败、来源hash和图像索引见 `.codex-temp/lq-s4-centered-report.md`、`lq-s4-auth-browser-report.md`，共享壳见 `lq-s4-shell-*`；总结果与后续门禁持续写入验收表。

后续实测：第三图回退6个独立用例已有首轮5项+补跑1项通过；S3开关独立保留、旧壳和原生登录/安全来源均实际验证。补跑只增加等待旧登录场景有限入场结束，未改对比阈值；报告 `lq-s4-rollback-report.md` 明确首轮两张过渡截图不是稳定视觉签字。新增壳4×CPU性能首轮4项失败，含需要修正的测试惰性安装基线与说明浮层收束，也包含手机端真实开关的>50ms布局任务，正在源码归因，不把它们统称为测试噪声。已测初始CLS最高0.003952，20轮学生双宽/教师手机的节点及资源无增长，教师桌面基线仍待纠正后复验。

第一包源码检查点 `lq-s4-first-package-source-checkpoint` 保存553条源码记录、825个图文件/sidecar，副本SHA核对0差异；它明确是性能未收口时的源码快照，不是阶段验收或数据库恢复点。为并行开发C1/D后包，性能候选从此快照及其414个经过SHA核实的静态输入单独构建，避免将未完成的后包文件混入第一包复验。

C1路由票已单独实现：显式appearance教师路径、双角色canonical href map、同一profile family控制入口与bootstrap，关闭时appearance回基础资料；保留教师鉴权、原query与其他角色fallback。隔离routing7项、原C0 9项、管理导航16项通过；前端外观原生控件/输入适配仍在接线中，尚未宣称真实偏好保存与整页呈现验收。

## 后续并行包与性能纠偏进度

2026-09-21 后续实测补记：C2.2 草稿/发送归属新增14项与原请求竞态15项同轮29/29通过；C2.3 AI轮询17/17通过。覆盖真实File/FormData归属、精确ObjectURL释放、scope分离、未知发送回执不重试、旧轮询不得清除新会话lease、永久错误停链与临时错误单链重试。这些使用真实controller/partial及受控HTTP回执，尚不是完整应用或视觉验收；actions和消息皮肤仍继续施工。

共享层关闭候选 `f055d68cd289477b575c50b5875893ebc15a02b360b20b5d7f8500a057d8ade1` 的78项组件测试通过，20次关闭的重复209元素样式重算全部消除，关闭事件中位数由约38/41ms降至25/27ms。严格student390性能依然失败：off/tinted的p95为72/112ms，>50ms真实任务为4/15条，其中关闭各1条；透明模式打开任务仍多，不据局部改进宣称整包性能通过。原图与追踪保留，后续归因与正式门禁分开。

C1/D2统一正式图 `a2c8a19809cf109e475031da523d3b4bd68a35c6579c57155bb502930a763c8c` 已构建，415个原生静态输入，入口gzip17577/18432且源指纹通过；Vite独立内容哈希产物另保留30个文件SHA。D2原生认证实际8/8首轮通过：无JS/模块404、1440/390、真实教师审批与无权限拒绝、旧会话失效、重设完成、审计仅增一次、安全来源与令牌重放拒绝均实测；12张原图已逐张检查，详见 `lq-s4-auth-native-browser-report.md`。C1实际浏览器仍在执行，会话不可用入口另属D3，不合并宣称B16全部关闭。

C1外观输入适配和双壳正文接线已形成独立源码包。保留240ms字段CAS队列，以原生radio/switch/chip表示偏好；跨URL分区保持真实导航链接。紧凑身份页头、新表单呈现、教师总览canonical和状态节点适配均按profile flag开启，关闭/未定义输出有C0 9项与新Jinja7项验证。新外观/CAS组合43项、后续图表+adapter16项及呈现4项分别通过，其中重复覆盖不累计为独立总数。正式图浏览器、真实保存/失败/冲突与签名业务仍待完成，详见 `lq-s4-profile-c1-step2-report.md`。

D2已有真实原生identity/setup/forgot路径，复用原认证锁、重读、审核及通知编排，不新增凭证模型。51个独立Python与3个JS用例通过；随后统一h1和错误唯一autofocus的17项定向检查通过。八个真实浏览器用例已编写、类型检查通过，尚未运行；D2图和独立合成运行时未冒用第一包性能图。会话过期模板尚无真实入口，一言仍待单独迁移，见 `lq-s4-auth-native-implementation-report.md` 和 `lq-s4-d-remainder-plan.md`。

C2先修真实消息入口的异步正确性，再迁呈现。请求key+代次、可见失败/重试、首次metadata与搜索交错已用真实Jinja/controller的15个截持响应浏览器场景验证。独立审查发现的首次bootstrap丢分类与私信loading误作空态均已补例修正。页面内按会话和scope保存草稿、发送快照精确结算、附件五个缺失端口、AI轮询lease正在验证；未改后端发送队列或增加自动POST重试，未声称整个消息业务已验收。

第一包性能仍未关闭。scroll-lock候选 `f098c3c1024ab632d35328277c77c7b8d06e9ff39045d576b9872af4228af8c9` 消掉一轮全页重算，手机off/tinted p95=96/120ms，但长任务28/26个。后续native开层批写+惰性焦点候选 `b0d6fc0d2c948cd232c4560328757f5b717b055955b0308058a5143b7866e0c2` 经core/React/通知70项及Shell47项后复测，手机p95=72/88ms，长任务10/9个，主要余项在关闭回焦。两轮CLS=0、20轮节点/资源/请求均无增长；50ms门限保持，原失败和trace保留。不能把延迟达标写成性能全过，也不能用新C1/D2源码解释第一包候选图结果。
