# DSH 改造本次收尾记录

按用户要求停止扩大范围，本次保存已实现代码、改进计划和可复核证据。此记录属于本地开发检查点，不是生产上线验收报告。

## 已实现

- 保留平台 Agent 前后端与任务接口，将执行后端改为隔离的官方 DSH 运行器，模型密钥由网关保管，平台操作使用当前用户的实时授权。
- 接入逐项审查的业务能力、任务文件、追问与补充说明、操作回执及异常恢复。用户身份、管理员权限、会话撤销和资源权限继续由平台原业务校验。
- 成绩公布、签章审批、空班级/未引用课程删除与合班采用本人确认，绑定实时影响快照；业务与回执同事务处理。
- 补充签章、账号、材料和教学资源并发保护，普通 Web 与 Agent 共用领域服务及确认界面。
- 子工作流原型及限额验证已保留；由于取消后远端 MCP 工作停止尚无完整证明，子代理与工作流插件继续禁用。

具体功能边界以 `agent-capability-matrix.md` 为准；完整调查与改进顺序见 `agent-dsh-migration-plan-2026-09-10.md`。登记能力数量不代表整个平台全部业务已经覆盖。

## 已验证与证据边界

- 本人确认组合后端 32 项通过；签章本人确认原生 PostgreSQL 并发 5 项通过。
- Agent 问答、编辑器及本人确认真实 Chrome 组件共 26 项通过；正常教学生命周期另有专项报告。组件测试使用受控接口响应，不代表生产整站验收。
- 前端类型检查和生产构建通过。
- 早期独立服务器环境中，教师、学生、管理员三条真实 DSH 任务完成，18 项业务及 8 项收尾/身份边界检查通过；该结果属于中间源码，不能替代最终冻结代码联测。
- 生产 custom dump 的中间源码迁移两遍通过；最终源码、备份与附件完全匹配的部署门禁尚未完成。
- 各历史报告保留其原始源码摘要与测试范围，不用最新代码摘要覆盖旧验收事实。

新运行镜像 `lanshare-dsh:0.1.5-rc.1-lanshare-20260910-c3` 已构建：image 为 `sha256:8037a4ea6d2d8760733f60b7a1a8222e7444f519bcf371b3734da86da1b7d911`，profile 为 `e4bdb76edddef2f20935e61bc4e32dc186f135da057e704b03332fdabecf0cd6`。官方 ACP/MCP、Shell、文件、问答、搜索代理、权限拒绝与取消检查通过；launcher 5 项、真实配额 4 项、Linux 单测 16 项及发布清单消费者 4 项全部通过。发布清单明确 `full_platform_e2e_for_image=false`；旧镜像和旧验收证据保留，构建器已停止，实验运行器无残留。详见 `agent-dsh-c3-validation-2026-09-10.json`。

最后一次全量 Agent 回归执行 633 项，耗时 102.683 秒：585 项通过、47 项原生数据库测试因未设置专用环境跳过、1 项旧作业创建单测报错。该测试的替身连接没有提供新增上下文校验需要的课程行；后续只修复测试夹具并重跑所属模块，不修改业务来迁就测试，也不将这次全量运行记为全过。日志为 `.codex-temp/dsh-release/agent-regression-c3-closeout.log`，SHA256 为 `356bf525af67d4623ce8b14ccffb44ee1305e7d9b177bbad96ae45709bf85cce`。

该夹具补齐后，整个 `test_agent_postgres_writes` 模块 6/6 通过、无跳过，业务代码未变。教学相关末轮 73/73、正常 Web Chrome 4/4 通过；来源摘要和验证范围保存在 `agent-teaching-lifecycle-verification-2026-09-10.json`。按用户收尾要求不再重跑整组或扩大测试。

能力台账三文件已冻结，生成及 `--check` 通过，来源变化和待复核均为 0；19 项 inventory 与 9 项 MCP 检查全部通过，`git diff --check` 通过。登记 197 项局部能力，其中本人业务确认 5 项；教学新增锁的原生 PostgreSQL 竞争仍为上线前阻断，不能以来源校验通过替代业务验收。

## 尚未上线与剩余步骤

生产仍为原发布 `20260910-012744-d83074a36d06`，当前应用与 PostgreSQL 健康，公网 `/api/internal/health` 正常。旧 DeepSeek-TUI 后端尚未在生产切换或退役。

继续上线前必须完成：最终冻结源码下的原生迁移门禁；新镜像与冻结平台代码的真实 DSH 角色、文件、追问、取消及异常联测；部署 dry-run、正式切换、线上专项验收及旧运行器退役。新增教学父资源/上下文锁尚未完成独立的真实 PostgreSQL 竞争验收，也应在生产启用前补齐。

尚未覆盖的业务、未激活的插件以及生产切换均不标记为完成。本次不发起新的付费模型任务、不切换生产、不删除已有验收环境或回滚镜像。
