# 课堂成员与签到归档生产发布记录

2026-09-13 已部署，版本 **`20260913-081814-27a3d6ff7be7`**。

发布源码：`a98914c3`，包含功能提交 `d3518a44` 和部署前 schema 门禁测试修复。分支为 `codex/classroom-attendance-archive`。本记录及 JSON 属于发布后的文档提交，不改变已部署运行时代码。

完整脱敏证据：[发布验证 JSON](classroom-attendance-release-2026-09-13.json)。

## 发布过程

1. 从生产 PostgreSQL 获取一致性 custom dump，远端与本地 SHA-256、大小一致；备份在 E 盘受限目录保存。
2. 在干净隔离工作区完成原生 PostgreSQL 增量及两次独立完整启动预演，旧数据、字段和序列保留，幂等差异为零；自有临时集群已停止并清理。
3. 通过匹配备份、迁移源码哈希及配置检查的 DryRun。
4. 使用现有部署脚本构建镜像；停止写入服务后再次备份，执行一次 schema 初始化。**168/168 必需表通过，索引失败 0，跳过步骤 0。**
5. 恢复全部 8 个服务；所有带健康检查的服务均 healthy，主服务及 AI 健康正常，持久任务 worker 已启动。

回退备份均为 `0600`：

- 代码：`/tmp/lanshare-deploy-backups/code-20260913-081825.tgz`
- 切换时数据库：`/tmp/lanshare-deploy-backups/db-cutover-20260913-081825.sql.gz`，gzip 完整性验证通过。

部署保护原有运行数据、环境文件与存储目录，未以预演数据库替换生产库。

## 验收结果

- **源码与公网**：917 个运行时源码文件与冻结版本完全一致；4 个新增 JS/CSS 公网文件哈希一致。HTTPS 健康页与容器内版本号一致。
- **真实课堂读取**：对课堂 ID 6（动态 Web 程序设计、软工 2401 班）的只读路由探针 **19/19 通过**。名册总数 37，2 人分页、搜索空结果、六页签壳及四个懒加载面板、归档菜单/列表/筛选和已保存来源读取均正常。
- **授权**：其他教师访问该课堂名册/来源被拒绝；相同数值 ID 的学生身份仍被拒绝。公网匿名归档 API 返回 401，页面转到教师登录。
- **缓存与文件**：新版本首次 HTML 仅发送 `Clear-Site-Data: "cache"`，第二次不重复发送；未清理浏览器存储或 cookie。三个已有全局存储文件的大小与哈希在切换前后完全一致。
- **数据库测试**：两项开发期已记录的 Agent schema 测试失败已修复，相关 **67/67 通过**。只调整测试对现有后置迁移的认识和替身精确 SQL 支持，未改动生产 Agent schema。
- **任务状态**：新导出与解析任务均为 queued/running/failed/stale = 0；当前各后台任务项均为 ok、stale = 0。总健康标记仍包含 511 条既有失败记录，不能将其表述为全历史任务均成功。

## 验收边界

生产尚无正式签到档案，本轮没有创建测试档案或重新触发源站/付费 AI，因此生产 PDF 下载、完整解析发布与档案特定跨教师访问未由新档案验证。既有真实 555 格源站及 AI 验证、本地 PostgreSQL PDF Range/HEAD 与并发测试仍作为各自层级的证据。

生产路由探针采用已存在的身份注入和 PostgreSQL 只读事务，不是登录浏览器全流程。独立进程先验证既有主班关联表、列、索引及回填状态，再设置其进程内就绪标记，避免只读探针重复执行惰性 DDL；旧学情重算在探针内关闭，真实缓存查询保留。首次探针因此产生的三处只读事务拒绝已经定位，修正探针后目标课堂 19 项通过，未修改生产业务代码。

未运行会写入权重、预警和结课状态的完整 P03 套件，也未执行生产混合负载测试。本轮发布验收新增模型调用 **0**，源站调用 **0**，业务测试写入 **0**。

上线入口：课堂“更多 → 成员”，以及[教务归档 → 签到统计表](https://guardianangel.net.cn/manage/archive/attendance-reports)。
